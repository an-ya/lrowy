<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrowy.dao.BookmarkDao">
    <insert id="save" parameterType="com.lrowy.pojo.bookmark.Bookmark">
        <selectKey keyProperty="bookmarkId" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO bookmark (description,title,faviconUrl,faviconBlurUrl,faviconOriginalUrl,state,url,baseUrl,visits,delFlag,hiddenFlag,isAccessible,shortcutFlag,createDate) VALUE (#{description},#{title},#{faviconUrl},#{faviconBlurUrl},#{faviconOriginalUrl},#{state},#{url},#{baseUrl},#{visits},#{delFlag},#{hiddenFlag},#{isAccessible},#{shortcutFlag},#{createDate});
    </insert>

    <select id="findShortcut" resultType="com.lrowy.pojo.bookmark.Bookmark">
        SELECT * FROM bookmark WHERE shortcutFlag = 1;
    </select>

    <select id="findBookmark" resultType="com.lrowy.pojo.bookmark.Bookmark">
        SELECT * FROM bookmark WHERE shortcutFlag = 0;
    </select>
</mapper>